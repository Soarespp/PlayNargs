import React from 'react';
import AuthOrApp from './authOrApp';

const Content = (props) => {
    return (
        <div className="App">
            <AuthOrApp />
        </div>
    );
}

export default Content;